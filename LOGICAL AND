A PROGRAM TO FIND RANGE BLW TWO NUMBERS USING LOGICAL AND OPERATOR
#include <stdio.h>

int main() {
    int a;
    int res;
    printf("Enter a number: ");
    scanf(" %d", &a);
    res=(a>='-4'&&a<='6') ;
    printf("The %d is in range from -4 to 6: %d\n", a, res);
    return 0;
}
